{
  "name": "bonjourr-lolicon-wallpaper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/gallery.js && node --check scripts/update-gallery.mjs",
    "deploy": "wrangler deploy",
    "update-gallery": "node scripts/update-gallery.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
